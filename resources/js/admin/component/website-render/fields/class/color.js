import mainFieldClass from "./mainFieldClass";

export default class color extends mainFieldClass {
    constructor(values, isSample = false, params = null, lang = null) {
        super(values, isSample, params, lang);
    }
}
